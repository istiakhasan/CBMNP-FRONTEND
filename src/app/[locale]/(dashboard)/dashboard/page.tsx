import React from 'react';
import Dashboard from './_component/Dashboard';
export const metadata = {
  title: "Dashboard",
};
const page = () => {

  return (
    <Dashboard />
  );
};

export default page;