import NotAuthorized from '@/components/NotAuthorized';
import React from 'react';

const Page = () => {
    return (
        <div>
            <NotAuthorized />
        </div>
    );
};

export default Page;